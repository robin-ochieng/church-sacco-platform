# Supabase Configuration
# Get these from: Supabase Dashboard > Settings > API

# Your Supabase project URL (Required - must be valid URI)
SUPABASE_URL=https://your-project-ref.supabase.co

# Anon (public) key - safe to use in browser (Required)
SUPABASE_ANON_KEY=your-anon-key-here

# Service role key - NEVER expose in client! Backend only (Required)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Direct Database URL (for Prisma migrations) (Required - must be valid PostgreSQL URI)
# Get from: Supabase Dashboard > Settings > Database > Connection string > URI
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.xxxxxxxxxxxxx.supabase.co:5432/postgres"

# Symmetric key for pgcrypto helper functions (Required - must be valid base64)
# Generate with: openssl rand -base64 32
PII_ENCRYPTION_KEY=change-me-32-byte-base64

# For production with connection pooling:
# DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# IMPORTANT: All required variables must be set for database migrations and seeding.
# Missing or invalid variables will cause migration failures.
# Required variables:
# - DATABASE_URL (valid PostgreSQL URI)
# - SUPABASE_URL (valid URI)
# - SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_ROLE_KEY
# - PII_ENCRYPTION_KEY (valid base64)
