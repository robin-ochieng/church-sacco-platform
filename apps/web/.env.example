# Web App Environment Variables

# API Backend URL
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# Teller dashboard timezone (IANA identifier, defaults to Africa/Nairobi)
NEXT_PUBLIC_BANK_TIMEZONE=Africa/Nairobi

# Supabase Configuration (Public - safe for client-side)
# Get these from: Supabase Dashboard > Settings > API

# Your Supabase project URL (Required - must be valid URI)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Anon (public) key - Safe to expose in browser (Required)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# IMPORTANT: All NEXT_PUBLIC_* variables must be set at build time.
# Missing required variables will cause runtime errors in the browser.
# Required variables:
# - NEXT_PUBLIC_API_URL (valid URI)
# - NEXT_PUBLIC_SUPABASE_URL (valid URI)
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
