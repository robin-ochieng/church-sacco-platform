# Test Environment Configuration
# This file should use a SEPARATE test database, never production!

# Supabase Test Project
SUPABASE_URL=your-test-supabase-url
SUPABASE_ANON_KEY=your-test-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-test-service-role-key

# Test Database (use separate test database or schema)
DATABASE_URL=postgresql://postgres:password@localhost:54322/postgres?schema=test

# JWT Configuration (use different secrets for testing)
JWT_SECRET=test-jwt-secret-key-change-in-production
JWT_REFRESH_SECRET=test-refresh-secret-key-change-in-production
JWT_EXPIRATION=1h

# Node Environment
NODE_ENV=test

# Instructions:
# 1. Copy your .env values here OR
# 2. Create a separate Supabase test project OR
# 3. Use a test schema in your existing database
#
# IMPORTANT: Never use production database for tests!
# Tests will create and delete data automatically.
